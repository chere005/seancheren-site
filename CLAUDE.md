# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The guidance itself lives in `AGENTS.md`, imported below, so that every agent
that reads this repo reads the same one file. Add to that, not to this.

@AGENTS.md
